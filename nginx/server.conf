upstream backend {
    server elena-bucatinschi-backend:4321;
}

server {
    listen 90;

    location / {
        proxy_set_header Host $http_host;
        proxy_pass http://backend;
    }

    location /static/ {
        alias /static/;
    }

    location /media/ {
        alias /media/;
    }
}
